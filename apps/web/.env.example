# API base URL used by the browser (client-side fetches).
# In dev: point to the local NestJS server.
# In k3d/prod: leave empty — the browser calls /api/hello through the ingress.
PUBLIC_API_URL=http://localhost:3000

# API base URL used server-side by Astro SSR (auth form handlers).
API_URL=http://localhost:3000
