apiVersion: v1
kind: Service
metadata:
  name: bitcoind
  namespace: crypto-demo
spec:
  type: ClusterIP
  selector:
    app: bitcoind
  ports:
    - name: rpc
      port: 18443
      targetPort: 18443
    - name: p2p
      port: 18444
      targetPort: 18444
